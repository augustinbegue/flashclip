{
  "name": "app",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "start": "bun run db:push && bun run src/index.ts",
    "build": "tsc",
    "test": "bun test",
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate dev",
    "db:push": "prisma db push",
    "db:studio": "prisma studio"
  },
  "dependencies": {
    "@hono/zod-validator": "^0.7.5",
    "@prisma/adapter-libsql": "^7.1.0",
    "@prisma/client": "latest",
    "aedes": "^0.51.3",
    "dotenv": "^17.2.3",
    "hono": "^4.10.7",
    "mime-types": "latest",
    "mqtt": "^5.14.1",
    "zod": "^4.1.13"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.0.0",
    "prisma": "latest",
    "@types/mime-types": "latest"
  }
}
