# ============================================================================
# Persistent Volume Claim - Flashclip Backend Database
# Generated by AGL CLI
# ============================================================================

apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: flashclip-backend-data
  namespace: flashclip
  labels:
    app: flashclip    
    component: backend
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
