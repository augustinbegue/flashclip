# ============================================================================
# Kubernetes Service - Flashclip Backend
# Generated by AGL CLI
# ============================================================================

apiVersion: v1
kind: Service
metadata:
  name: flashclip-backend
  namespace: flashclip
  labels:
    app: flashclip
    component: backend
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
    - name: mqtt
      port: 1883
      targetPort: mqtt
      protocol: TCP
  selector:
    app: flashclip
    component: backend
