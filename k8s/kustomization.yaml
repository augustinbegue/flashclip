# ============================================================================
# Kustomization - Flashclip# Generated by AGL CLI
# ============================================================================

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: flashclip
resources:
  - pvc.yaml
  - pvc-processing.yaml
  # - secret-processing.yaml
  - deployment.yaml
  - deployment-processing.yaml
  - deployment-web.yaml
  - service.yaml
  - service-processing.yaml
  - service-web.yaml
  - ingress-route.yaml

commonLabels:
  app.kubernetes.io/name: flashclip
  app.kubernetes.io/managed-by: agl-cli
